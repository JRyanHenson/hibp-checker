name: CI/CD — hibp-webapp

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly rebuild pulls latest base image security patches
    - cron: '0 0 * * 0'

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: hibp-webapp
  ECS_SERVICE: hibp-webapp-service
  ECS_CLUSTER: whoami-site-cluster

jobs:

  # ── GATE 1: Python SAST ──────────────────────────────────────
  # Bandit scans Python source for common security issues:
  # hardcoded passwords, use of insecure functions, shell injection
  sast:
    name: SAST (Bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install bandit

      - name: Run Bandit
        # -ll = medium and high severity only (skip low noise)
        # -r  = recursive
        run: bandit -ll -r app.py

  # ── GATE 2: Dependency scan ──────────────────────────────────
  # pip-audit checks requirements.txt against known CVEs in the
  # Python Advisory Database. Catches vulnerable Flask, requests,
  # gunicorn versions before they reach production.
  dependency-scan:
    name: Dependency Scan (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install pip-audit

      - name: Audit dependencies
        run: pip-audit -r requirements.txt

  # ── GATE 3: Secrets detection ────────────────────────────────
  # Gitleaks scans the full git history for leaked credentials.
  # fetch-depth: 0 is required — without it only the latest commit
  # is scanned and secrets buried in history go undetected.
  secrets-scan:
    name: Secrets Detection (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── TESTS ────────────────────────────────────────────────────
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: pytest -v

  # ── BUILD → SCAN → DEPLOY ────────────────────────────────────
  # Only runs when ALL four gates above pass.
  # Only deploys on push to main — not on PRs or scheduled runs.
  build-and-deploy:
    name: Build, Scan, and Deploy
    runs-on: ubuntu-latest
    needs: [sast, dependency-scan, secrets-scan, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build --pull \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .

      - name: Scan container image (Trivy)
        # Scans OS packages and Python packages inside the final
        # image for known CVEs. Fails the pipeline on CRITICAL or
        # HIGH findings — image is never pushed if this fails.
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
          exit-code: '1'
          severity: 'CRITICAL,HIGH'
          format: 'table'

      - name: Push to ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service $ECS_SERVICE \
            --force-new-deployment
